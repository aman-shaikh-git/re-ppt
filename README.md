# re-ppt
RePPT is a Python-based utility designed to automate the creation of repetitive PowerPoint slides by mapping template shapes to an Excel interface. It uses the native PowerPoint COM API to preserve formatting and slide integrity.
